/**
 * @deprecated RoomsPermissionService has been moved to the `permission/` module
 * as `BoardPermissionService`. Import from `../permission` instead.
 * This shim remains for backward compatibility during the refactor.
 */
export {
  BoardPermissionService as RoomsPermissionService,
} from "../permission/services/board-permission.service"
